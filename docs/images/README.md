# Screenshots for README

Add these images before publishing to improve first impressions:

- **overlay.png** — Screenshot of the overlay search view (hotkey or menu bar → search field and clipboard history list).
- **preferences.png** — Screenshot of the Preferences panel (menu bar → Preferences…).

Capture with the app running, then place the PNG files in this directory. The main [README](../../README.md) can reference them in a Screenshots section (e.g. `docs/images/overlay.png`, `docs/images/preferences.png`).
